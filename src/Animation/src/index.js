class Animation {
    /**
     * Animates a number.
     *
     * @param {number} initial - The initial value of the number.
     * @param {number} final - The final value of the number.
     * @param {number} speed - The number of seconds for the animation to take place.
     * @param {function} callback - The function to call at every frame of the animation.
     */
    animate(initial, final, speed, callback) {
        
    }
}

export default Animation;
